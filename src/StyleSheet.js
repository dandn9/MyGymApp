import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  input: {
    borderColor: "#000",
    borderWidth: 2,
    borderStyle: "solid",
  },
});

export default styles;
